DATABASE_URL=postgres://booking:booking@localhost:5432/booking
PORT=3000
# NODE_ENV=production
# CORS_ALLOWED_ORIGINS=https://your-client-domain.com,https://another-allowed-origin.com
# Optional — only set this if you're running a local OTLP collector (e.g. via
# docker-compose). Leave unset to skip OpenTelemetry entirely.
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
