{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/main.ts",
    "build": "vp pack",
    "build:openapi": "node scripts/generate-openapi.ts",
    "check": "vp check && knip",
    "start": "node dist/main.mjs",
    "test": "vp test"
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "catalog:",
    "express": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/express": "catalog:",
    "@types/node": "catalog:",
    "@types/supertest": "catalog:",
    "@types/swagger-ui-express": "catalog:",
    "msw": "catalog:",
    "supertest": "catalog:",
    "swagger-ui-express": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  }
}
