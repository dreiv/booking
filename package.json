{
  "name": "booking",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "dev": "pnpm --filter client --filter server --parallel dev",
    "dev:client": "pnpm --filter client",
    "dev:server": "pnpm --filter server",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.14.0"
}
