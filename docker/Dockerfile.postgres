FROM postgres:17-bookworm

# postgresql-17-cron is available via the PGDG apt repo already configured
# in the base image.
RUN apt-get update \
    && apt-get install -y --no-install-recommends postgresql-17-cron \
    && rm -rf /var/lib/apt/lists/*
